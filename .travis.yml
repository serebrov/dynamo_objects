language: python
python: "2.7"
env:
  - TOXENV=py27
  - TOXENV=py33
  - TOXENV=py34
  - TOXENV=py35
  - TOXENV=pypy
install:
  - pip install -r requirements.txt
  - pip install tox coveralls --use-mirrors
script: tox
after_success:
  if [ "$TOXENV" == "py27" ]; then coveralls; fi
